version: 1.0.3
description: UPF initial local configuration

# PFCP Configuration
# The listen IP and nodeID of the N4 interface on this UPF (Can't set to 0.0.0.0)
pfcp:
  addr: 5.5.5.2   # IP addr for listening
  nodeID: 5.5.5.2 # External IP or FQDN can be reached
  retransTimeout: 1s # retransmission timeout
  maxRetrans: 3 # the max number of retransmission

# GTP-U Configuration
gtpu:
  forwarder: gtp5g
  # The IP list of the N3/N9 interfaces on this UPF
  # You can bind this to a specific IP from your available subnets (e.g., 10.10.0.1).
  # Alternatively, you can bind it to all interfaces using 0.0.0.0.
  # If you bind to a specific IP, ensure SMF uses the same IP in its N3 configuration.
  # If you bind to all (0.0.0.0), SMF can use any of the available UPF IPs, but do not use 0.0.0.0 in SMF.
  ifList:
    - addr: 5.5.5.2
      type: N3
      # name: upf.5gc.nctu.me
      # ifname: gtpif
      # mtu: 1400

# Supported DNNs
# List of Data Network Names (DNN) supported by this UPF.
dnnList:
  - dnn: internet # Data Network Name
    cidr: 10.61.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: internet # Data Network Name
    cidr: 10.71.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: internet # Data Network Name
    cidr: 10.81.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: internet # Data Network Name
    cidr: 10.91.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: ims # Data Network Name
    cidr: 10.62.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: ims # Data Network Name
    cidr: 10.72.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: ims # Data Network Name
    cidr: 10.82.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: ims # Data Network Name
    cidr: 10.92.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: wnctest # Data Network Name
    cidr: 10.63.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: wnctest # Data Network Name
    cidr: 10.73.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: wnctest # Data Network Name
    cidr: 10.83.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: wnctest # Data Network Name
    cidr: 10.93.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: vzwadmin # Data Network Name
    cidr: 10.64.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: vzwadmin # Data Network Name
    cidr: 10.74.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: vzwadmin # Data Network Name
    cidr: 10.84.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: vzwadmin # Data Network Name
    cidr: 10.94.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.65.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.75.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.85.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.95.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    # natifname: eth0

# Logging Configuration
# Controls logging level and output for the UPF.
logger:
  enable: true # true or false
  level: trace # how detailed to output, value: trace, debug, info, warn, error, fatal, panic
  reportCaller: false # enable the caller report or not, value: true or false

version: 1.0.3
description: UPF initial local configuration

# PFCP Configuration
# The listen IP and nodeID of the N4 interface on this UPF (Can't set to 0.0.0.0)
pfcp:
  addr: 5.5.5.2   # IP addr for listening
  nodeID: 5.5.5.2 # External IP or FQDN can be reached
  retransTimeout: 1s # retransmission timeout
  maxRetrans: 3 # the max number of retransmission

# GTP-U Configuration
gtpu:
  forwarder: gtp5g
  # The IP list of the N3/N9 interfaces on this UPF
  # You can bind this to a specific IP from your available subnets (e.g., 10.10.0.1).
  # Alternatively, you can bind it to all interfaces using 0.0.0.0.
  # If you bind to a specific IP, ensure SMF uses the same IP in its N3 configuration.
  # If you bind to all (0.0.0.0), SMF can use any of the available UPF IPs, but do not use 0.0.0.0 in SMF.
  ifList:
    - addr: 5.5.5.2
      type: N3
    - addr: 2001:db8::5:5:5:2
      type: N3
      # name: upf.5gc.nctu.me
      # ifname: gtpif
      # mtu: 1400

# Supported DNNs
# List of Data Network Names (DNN) supported by this UPF.
dnnList:
  - dnn: fast.t-mobile.com # Data Network Name
    cidr: 10.111.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    ipv6:
      prefix: 2001:db8:0111::/48
      uePrefixLength: 64
      allocation: random
      raProfile: default
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      #sd: "000001"
  - dnn: fast.t-mobile.com # Data Network Name
    cidr: 10.121.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000001"
  - dnn: fast.t-mobile.com # Data Network Name
    cidr: 10.131.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    ipv6:
      prefix: 2001:db8:0131::/48
      uePrefixLength: 64
      allocation: random
      raProfile: default
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "0007D0"
  - dnn: fast.t-mobile.com # Data Network Name
    cidr: 10.141.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    ipv6:
      prefix: 2001:db8:0141::/48
      uePrefixLength: 64
      allocation: random
      raProfile: default
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000BB8"
  - dnn: fast.t-mobile.com # Data Network Name
    cidr: 10.151.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "050601"
  - dnn: fast.t-mobile.com # Data Network Name
    cidr: 10.161.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 2
      sd: "050602"
  - dnn: fast.t-mobile.com # Data Network Name
    cidr: 10.171.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 3
      sd: "050603"
  - dnn: internet # Data Network Name
    cidr: 10.112.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      #sd: "000001"
  - dnn: internet # Data Network Name
    cidr: 10.122.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000001"
  - dnn: internet # Data Network Name
    cidr: 10.132.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "0007D0"
  - dnn: internet # Data Network Name
    cidr: 10.142.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000BB8"
  - dnn: internet # Data Network Name
    cidr: 10.152.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "050601"
  - dnn: internet # Data Network Name
    cidr: 10.162.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 2
      sd: "050602"
  - dnn: internet # Data Network Name
    cidr: 10.172.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 3
      sd: "050603"
  - dnn: ims # Data Network Name
    cidr: 10.113.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      #sd: "000001"
  - dnn: ims # Data Network Name
    cidr: 10.123.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000001"
  - dnn: ims # Data Network Name
    cidr: 10.133.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "0007D0"
  - dnn: ims # Data Network Name
    cidr: 10.143.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000BB8"
  - dnn: ims # Data Network Name
    cidr: 10.153.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    ipv6:
      prefix: 2001:db8:0153::/48
      uePrefixLength: 64
      allocation: random
      raProfile: default
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "050601"
  - dnn: ims # Data Network Name
    cidr: 10.163.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 2
      sd: "050602"
  - dnn: ims # Data Network Name
    cidr: 10.173.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 3
      sd: "050603"
  - dnn: sos # Data Network Name
    cidr: 10.114.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      #sd: "000001"
  - dnn: sos # Data Network Name
    cidr: 10.124.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000001"
  - dnn: sos # Data Network Name
    cidr: 10.134.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "0007D0"
  - dnn: sos # Data Network Name
    cidr: 10.144.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000BB8"
  - dnn: sos # Data Network Name
    cidr: 10.154.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "050601"
  - dnn: sos # Data Network Name
    cidr: 10.164.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 2
      sd: "050602"
  - dnn: sos # Data Network Name
    cidr: 10.174.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 3
      sd: "050603"
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.115.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      #sd: "000001"
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.125.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000001"
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.135.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "0007D0"
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.145.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000BB8"
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.155.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    ipv6:
      prefix: 2001:db8:0155::/48
      uePrefixLength: 64
      allocation: random
      raProfile: default
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "050601"
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.165.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 2
      sd: "050602"
  - dnn: V5GA01INTERNET # Data Network Name
    cidr: 10.175.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 3
      sd: "050603"
  - dnn: vzwadmin # Data Network Name
    cidr: 10.116.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      #sd: "000001"
  - dnn: vzwadmin # Data Network Name
    cidr: 10.126.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000001"
  - dnn: vzwadmin # Data Network Name
    cidr: 10.136.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "0007D0"
  - dnn: vzwadmin # Data Network Name
    cidr: 10.146.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000BB8"
  - dnn: vzwadmin # Data Network Name
    cidr: 10.156.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    ipv6:
      prefix: 2001:db8:0156::/48
      uePrefixLength: 64
      allocation: random
      raProfile: default
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "050601"
  - dnn: vzwadmin # Data Network Name
    cidr: 10.166.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 2
      sd: "050602"
  - dnn: vzwadmin # Data Network Name
    cidr: 10.176.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 3
      sd: "050603"
  - dnn: wnctest # Data Network Name
    cidr: 10.117.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      #sd: "000001"
  - dnn: wnctest # Data Network Name
    cidr: 10.127.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000001"
  - dnn: wnctest # Data Network Name
    cidr: 10.137.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "0007D0"
  - dnn: wnctest # Data Network Name
    cidr: 10.147.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "000BB8"
  - dnn: wnctest # Data Network Name
    cidr: 10.157.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 1
      sd: "050601"
  - dnn: wnctest # Data Network Name
    cidr: 10.167.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 2
      sd: "050602"
  - dnn: wnctest # Data Network Name
    cidr: 10.177.0.0/16 # Classless Inter-Domain Routing for assigned IPv4 pool of UE
    snssai:              # OPTIONAL - only for logging, not routing logic
      sst: 3
      sd: "050603"

    # natifname: eth0

# Router Advertisement profiles for IPv6
routerAdvertisements:
  default:
    enable: true
    routerAddress: fe80::1
    prefixLength: 64
    linkMtu: 1500
    flags: 0xC0  # Managed address configuration (M) and Other configuration (O)
    lifetime: 1800
    reachableTimer: 0
    retransTimer: 0
    dns:
      - 2001:4860:4860::8888
      - 2001:4860:4860::8844

# HTTP Service Configuration
# WNC: HTTP server for Router Advertisement (RA) injection endpoint
# This setting controls whether the UPF starts an HTTP server to receive RA packets from SMF.
# The delivery method (HTTP vs PFCP) is controlled by SMF configuration, NOT here.
#
# enable: true  - Start HTTP server, SMF can deliver RAs via HTTP (recommended for now)
# enable: false - No HTTP server, SMF HTTP delivery will fail with connection refused
#                 Note: Setting this to false does NOT make SMF use PFCP instead.
#                 To use PFCP delivery, configure SMF's routerAdvertisement.deliveryMethod
#                 (PFCP delivery not yet implemented - Phase 3.2+)
httpService:
  enable: true   # Required when SMF uses HTTP delivery method (current default)
  port: 8080     # HTTP server port
  addr: 5.5.5.2  # Listen address for HTTP service

# Logging Configuration
# Controls logging level and output for the UPF.
logger:
  enable: true # true or false
  level: trace # how detailed to output, value: wnc_trace, trace, debug, info, warn, error, fatal, panic
  reportCaller: false # enable the caller report or not, value: true or false
